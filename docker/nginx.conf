pid /nginx.pid;
events {
}
http {
    include mime.types;
    server {
        listen 80;
        server_name %s;
        include /app/docker/backend.nginx.conf;
        include /app/docker/frontend.nginx.conf;
        client_max_body_size 1024M;
    }
}